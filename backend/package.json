{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
  "dev": "bun run --hot src/index.ts",
  "start": "bun run src/index.ts",
  "build": "mkdir -p dist && echo 'API is running' > dist/index.html"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.10.0",
    "@vercel/node": "^5.0.0"
  }
}
